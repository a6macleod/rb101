['ant', 'bear', 'caterpillar'].pop.size

# returns the last element (or nil for an empty array) and returns it. This mutates the collection

# return value is the string 'caterpillar'
# chaining #size onto it will return 11 which is the length of the string
